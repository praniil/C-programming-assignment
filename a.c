#include <stdio.h>
int main()
#define PI 3.14
{
    int radius , area ;
    printf ("enter the radius of circle :");
    scanf ("%3d", &radius );
    area = PI * (radius * radius);
    printf ("the radius of the circle is %3d ", area );
    
}